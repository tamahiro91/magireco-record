# 開発ルール(AIアシスタント引き継ぎ用)

このリポジトリはClaude Code / ChatGPT / その他どのAIアシスタント(またはツールなしの手動編集)からでも、同じ流れで作業を引き継げるようにするためのメモです。新しくこのリポジトリを触るときは、まずこのファイルを読んでください。

## プロジェクト概要

- パチスロ「マギアレコード」の実戦データを記録する個人用Webアプリ
- 単一HTMLファイル `マギレコ記録.html` に HTML/CSS/JS がすべて入っている(ビルドツールなし)
- GitHub Pagesでホスティング: https://tamahiro91.github.io/magireco-record/
- 個人プロジェクトなので **`main` ブランチに直接コミットしてOK。PRは不要**
- 所有者は個人利用者本人(tamahiro91)。要望は基本的に日本語で来る

## ファイル構成

```
マギレコ記録.html   … アプリ本体(HTML + CSS + JS、全部このファイル)
index.html          … マギレコ記録.htmlへのリダイレクトのみ
manifest.json        … PWA用マニフェスト
icon-180/192/512.png … PWAアイコン
cards/                … エンディング中カード画像(ユーザー提供スクショから切り出し)
screens/              … ビッグ/at終了画面の選択肢サムネイル(同上)
```

## 技術構成のポイント

- vanilla JS、IIFEで1つの `<script>` タグ内に全ロジックが入っている(`(function () { ... })();`)
- Firebase Firestoreを使ったクラウド同期は、別の `<script type="module">` タグ(ファイル冒頭)。メインロジックとは独立していて、Firebase importが失敗してもメインアプリは動く
- 永続化は `localStorage`、キーは `magireko.visits.v1`(配列で来店記録一式を保存)
- 旧データ形式からの移行は `normalizeEntry()` に集約。フィールド名を変えたりデータ構造を変えたときは、ここに移行処理を足すこと(既存ユーザーのlocalStorageを壊さないため)
- 画面の出し分けは `updateConditionalFields()` が一括管理(BIG選択時だけ画面Aを表示、エンブリオ選択時だけ画面Bを表示、など)。新しい条件付きフィールドを足すときはここに追記する
- 長い選択肢(設定示唆・ビッグ終了画面など)は下からせり上がる共通ピッカー `openPicker(title, options, currentValue, onSelect)` を使う。多くの箇所で使い回されているので、拡張するときは他の呼び出し元を壊さないよう注意(例: `opt.img` がある時だけ画像を出す、のように既存の挙動を条件分岐で保つ)
- 短い選択肢は `buildToggleField` / `buildMultiToggleField`、ピッカー系のフィールドは `buildPatternPickerField` / `buildOptionsPickerField` など、共通ヘルパーを再利用する

## 開発フロー(毎回これに沿う)

1. **要望を聞く** → 画面の変更やデータ構造の変更を伴う場合、着手前にASCIIアートで画面イメージを提示し、曖昧な点があれば質問してから実装する(ユーザーの希望する進め方)
2. **実装**
3. **構文チェック**: `<script>` 部分を抜き出して `node --check` に通す
4. **実際にブラウザで動作確認**(下記「テストの仕方」参照)。見た目の変更はスクリーンショットで確認する
5. **バージョン表記を更新**: `.app-version` の中身(例 `v2026-09-14x`)を1つ進める。同日内は末尾のアルファベットを進める(`x`→`y`→`z`→`za`→`zb`…)。日付が変わったら日付部分を更新して`a`から再開
6. **git同期してコミット・プッシュ**
   ```
   git pull origin main --ff-only && git status
   git add "マギレコ記録.html" (変更したファイル)
   git commit -m "日本語で変更内容と理由を簡潔に"
   git push -u origin main
   ```
   - コミット前に必ず `git pull --ff-only` してから作業結果を確認すること
   - コミットメッセージは日本語、「何を・なぜ」を簡潔に

## テストの仕方

- Playwright + Chromiumを使う: `NODE_PATH=/opt/node22/lib/node_modules node script.js`、ブラウザ実行ファイルは `/opt/pw-browsers/chromium`
- **重要な注意点**: このアプリにはフォーカス時に自動スクロールするグローバルリスナーがあり、Playwrightの通常の `page.click()` は動的に生成された要素(カード内のボタン、ピッカーの選択肢など)に対して不安定になることがある。代わりに `page.evaluate(() => element.click())` でページ内から直接 `.click()` を呼ぶこと
- `localStorage` にテストデータを事前投入してから `page.reload()` する方法は、この環境だとまれに読み込みタイミングの問題で失敗することがある(実際のユーザー操作では起きない、テスト環境特有の問題)。確実なのは、FAB(＋)ボタンや各フォームをUI操作で実際にたどってデータを作る方法
- 画面の見た目を変更したときは `page.screenshot()` で撮って目視確認する

## 画像素材について

- ゲーム内の画像(カード、終了画面など)は著作権のあるファンサイト画像をそのまま使わず、**ユーザーが自分でアップロードしたスクリーンショットから切り出して使用する**運用にしている(ユーザー本人の合意済み方針)
- 切り出し手順: Pythonの `Pillow` でグリッド線入りデバッグ画像を作って座標を確認 → `im.crop(box)` → リサイズしてJPEG保存 → 目視で確認してからリポジトリにコミット

## データについて

- ユーザーのメールアドレスなど個人情報は扱わない、記録データはローカル(+任意でFirebase同期)のみ
- 機微な変更(データ構造の破壊的変更、削除機能など)はユーザーに確認してから実装する
